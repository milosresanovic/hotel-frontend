import { PriceColorPipePipe } from './price-color-pipe.pipe';

describe('PriceColorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PriceColorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
